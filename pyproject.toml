[project]
name = "stegentity"
version = "0.1.0"
description = "Platform-agnostic governed execution runtime for StegVerse entities."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
