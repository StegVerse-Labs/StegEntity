# Maintenance Capsules

A maintenance capsule is a portable proposed-change package containing target adapter, transition class, admissibility decision, operations, dependencies, consequences, rollback plan, and verification rules.
